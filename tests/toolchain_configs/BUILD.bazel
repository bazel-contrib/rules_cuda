load("@bazel_skylib//rules:write_file.bzl", "write_file")
load("@rules_cc//cc:defs.bzl", "cc_import", "cc_library")
load("//cuda:defs.bzl", "cuda_toolkit_info")
load(":environment_test.bzl", "runtime_libraries_test")

# Analysis-only archive fixtures; no compiler or GPU is needed by these tests.
[
    write_file(
        name = "write_" + name,
        out = name,
        content = [],
    )
    for name in [
        "libcudadevrt.a",
        "cudadevrt.lib",
        "libculibos.a",
    ]
]

cc_library(
    name = "linux_runtime",
    srcs = [":libcudadevrt.a"],
)

cc_import(
    name = "windows_runtime",
    interface_library = ":cudadevrt.lib",
    system_provided = True,
)

cuda_toolkit_info(
    name = "toolkit",
    bin2c = "//cuda/dummy:bin2c",
    device_runtime_static_libs = [
        ":linux_runtime",
        ":windows_runtime",
        ":libculibos.a",
    ],
    fatbinary = "//cuda/dummy:fatbinary",
    link_stub = "//cuda/dummy:link.stub",
    nvlink = "//cuda/dummy:nvlink",
    path = "C:/CUDA",
    ptxas = "//cuda/dummy:ptxas",
    version = "12.6",
)

runtime_libraries_test(
    name = "runtime_libraries_test",
    toolkit = ":toolkit",
)
