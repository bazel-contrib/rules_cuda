load("@rules_cuda//cuda:defs.bzl", "cuda_binary", "cuda_library")

cuda_library(
    name = "cuda_lib",
    srcs = ["lib.cu"],
    hdrs = ["lib.h"],
)

cuda_binary(
    name = "cuda_test",
    srcs = ["main.cu"],
    deps = [":cuda_lib"],
)
